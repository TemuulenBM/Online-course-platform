import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Миний сургалтууд' };

export default function TeacherCoursesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
